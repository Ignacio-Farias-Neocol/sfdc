<aura:component controller="HotListConversionController" implements="force:lightningQuickActionWithoutHeader,force:hasRecordId" access="global" >
    <lightning:navigation aura:id="navigationService" />
    <aura:handler name="init" action="{!c.doInit}" value="{!this}"/>
    <aura:html tag="style">
        .slds-backdrop {
            background: rgba(43, 40, 38, 0) !important;  
        }
        .slds-fade-in-open {
            display:none !important;
        }
    </aura:html>
   
    <!--
    <div class="slds-modal__header">
        <h2 class="slds-text-heading_medium slds-hyphenate">Convert to Opportunity</h2>
    </div>
    <div class="slds-modal__content slds-p-around_medium" id="modal-content-id-1">
        <p>You are about to convert Hotlist to Opportunity.</p>
        <br/>
        <p>Are you sure you want to continue?</p>
    </div>
    <div class="slds-modal__footer">
        <lightning:button class="slds-button_neutral" onclick="{!c.doInit}" label="Confirm" />
        <lightning:button class="slds-button_brand" onclick="{!c.cancel}" label="Cancel"/>
    </div>
`   -->
    <!--
    <div class="demo-only" style="height: 4rem;">
        <div class="slds-notify_container slds-is-relative">
            <div class="slds-notify slds-notify_toast slds-theme_success" role="status">
                <span class="slds-assistive-text">success</span>
                <span class="slds-icon_container slds-icon-utility-success slds-m-right_small slds-no-flex slds-align-top" title="Description of icon when needed">
                    <lightning:icon variant="inverse" iconName="utility:success" size="small"/>
                </span>
                <div class="slds-notify__content">
                <h2 class="slds-text-heading_small ">Hot List has been marked primary. Please convert the Lead.</h2>
                </div>
            </div>
        </div>
    </div>
   -->
</aura:component>
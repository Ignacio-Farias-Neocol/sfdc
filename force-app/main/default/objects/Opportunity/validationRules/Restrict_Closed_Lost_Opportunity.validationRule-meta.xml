<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Restrict_Closed_Lost_Opportunity</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
NOT($Permission.BYPASS_ALL_VALIDATIONS_ON_OPPTY), 
ISPICKVAL(PRIORVALUE(StageName), &apos;Closed Lost&apos;),
OR(ISCHANGED(StageName),
ISCHANGED(CloseDate)),
OR(CloseDate  &lt;=   (TODAY() -90),
PRIORVALUE(CloseDate) &lt;= (TODAY() -90))
)</errorConditionFormula>
    <errorMessage>THIS IS NOT A BUG.  A Closed Lost Opportunity cannot be reopened after 90 days.</errorMessage>
</ValidationRule>

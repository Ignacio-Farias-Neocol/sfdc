<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Closed_Lost_Competitor</fullName>
    <active>true</active>
    <description>If close lost reason is Lost to competitor / Kept current product, fill out the Lost Competitor field.</description>
    <errorConditionFormula>AND(
OR(NOT(Bypass_Validation__c), 
NOT($Permission.BYPASS_ALL_VALIDATIONS_ON_OPPTY )),
ISPICKVAL(StageName, &quot;Closed Lost&quot;),

ISPICKVAL(Closed_Lost_Reason__c , &quot;Lost to competitor / Kept current product&quot;),

ISBLANK(TEXT(Closed_Lost_Competitor__c ))
)</errorConditionFormula>
    <errorDisplayField>Closed_Lost_Competitor__c</errorDisplayField>
    <errorMessage>Please specify the Closed Lost Competitor.</errorMessage>
</ValidationRule>

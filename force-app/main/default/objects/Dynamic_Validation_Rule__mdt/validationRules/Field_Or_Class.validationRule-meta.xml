<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Field_Or_Class</fullName>
    <active>true</active>
    <description>Can only reference a Field or a Class, but not both.</description>
    <errorConditionFormula>AND(
  NOT(ISBLANK(Field_API_Name__c)),
  NOT(ISBLANK(Apex_Class_Name__c))
)</errorConditionFormula>
    <errorMessage>You can only reference a Field or a Class, not both.</errorMessage>
</ValidationRule>
